# CDK application dependencies (infrastructure synthesis/deployment).
# Pinned to exact tested versions for reproducible `cdk synth` / `cdk deploy`.
# aws_cdk.aws_s3files (L1 constructs for Amazon S3 Files) requires a recent CDK.
aws-cdk-lib==2.266.0
constructs==10.8.1
