[pytest]
# Only collect the project's unit tests. In particular, never recurse into
# cdk.out/, which contains Lambda bundles with Linux-built numpy/onnxruntime
# that must not be imported by the host test run.
testpaths = tests
norecursedirs = cdk.out .venv .cdk.staging models_local build dist *.egg
