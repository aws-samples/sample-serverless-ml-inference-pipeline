# Local development / testing / model-generation dependencies.
# Pinned to exact tested versions for reproducible synthesis and tests.
# Note: these local tool versions may be newer than the Lambda *runtime* bundle
# (lambdas/*/requirements.txt), which is capped more conservatively on purpose.
-r requirements.txt

pytest==9.1.1
numpy==2.5.2
# [tracer] extra installs aws-xray-sdk, required by the inference handler's Tracer.
aws-lambda-powertools[tracer]==3.34.0

# Used by scripts/generate_model.py to build the sample ONNX models,
# and to run the inference handler locally.
onnx==1.22.0
onnxruntime==1.29.0
